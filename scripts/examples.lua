function exp_x(x, alpha)
    return math.exp(x)
end

function exp_negx_sq(x, alpha)
    return math.exp(-x ^ 2)
end

function sine_x(x, alpha)
    return math.sin(x)
end

function foo(x, alpha)
    return x * math.sin(4 * x)
end
